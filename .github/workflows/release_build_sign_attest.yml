name: Build, Sign, and Attest Release Artifacts

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      release_tag:
        description: "Existing release tag to attach assets to (for manual reruns)."
        required: false
        type: string

jobs:
  build-sign-attest:
    name: ${{ matrix.platform }} release artifact
    runs-on: ${{ matrix.os }}
    continue-on-error: ${{ matrix.optional }}
    permissions:
      contents: write
      id-token: write
      attestations: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            platform: windows
            optional: false
          - os: macos-latest
            platform: macos
            optional: true
          - os: ubuntu-22.04
            platform: linux
            optional: false

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install packaging dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install pyinstaller PySide6 rasterio numpy Pillow

      - name: Build Windows bundle
        if: matrix.platform == 'windows'
        shell: pwsh
        run: |
          powershell -NoProfile -ExecutionPolicy Bypass -File scripts/package_windows.ps1

      - name: Build macOS bundle
        if: matrix.platform == 'macos'
        shell: bash
        run: |
          bash scripts/package_macos.sh

      - name: Build Linux bundle
        if: matrix.platform == 'linux'
        shell: bash
        run: |
          bash scripts/package_linux.sh

      - name: Archive Windows bundle
        if: matrix.platform == 'windows'
        shell: pwsh
        run: |
          Compress-Archive -Path dist/SatelliteUpscale -DestinationPath dist/SatelliteUpscale-windows.zip -Force

      - name: Archive macOS bundle
        if: matrix.platform == 'macos'
        shell: bash
        run: |
          if [ -d "dist/SatelliteUpscale.app" ]; then
            ditto -c -k --sequesterRsrc --keepParent dist/SatelliteUpscale.app dist/SatelliteUpscale-macos.zip
          else
            ditto -c -k --keepParent dist/SatelliteUpscale dist/SatelliteUpscale-macos.zip
          fi

      - name: Archive Linux bundle
        if: matrix.platform == 'linux'
        shell: bash
        run: |
          tar -C dist -czf dist/SatelliteUpscale-linux.tar.gz SatelliteUpscale

      - name: Generate checksums (Windows)
        if: matrix.platform == 'windows'
        shell: pwsh
        run: |
          python scripts/generate_release_checksums.py --dist-dir dist --pattern "SatelliteUpscale-${{ matrix.platform }}*" --output "dist/SHA256SUMS-${{ matrix.platform }}.txt"

      - name: Generate checksums (macOS/Linux)
        if: matrix.platform != 'windows'
        shell: bash
        run: |
          python scripts/generate_release_checksums.py \
            --dist-dir dist \
            --pattern "SatelliteUpscale-${{ matrix.platform }}*" \
            --output "dist/SHA256SUMS-${{ matrix.platform }}.txt"

      - name: Install Cosign
        uses: sigstore/cosign-installer@v4.0.0

      - name: Sign artifacts (keyless)
        if: matrix.platform != 'windows'
        shell: bash
        run: |
          set -euo pipefail
          for file in dist/SatelliteUpscale-${{ matrix.platform }}* dist/SHA256SUMS-${{ matrix.platform }}.txt; do
            cosign sign-blob --yes --bundle "${file}.sigstore.json" "$file"
          done

      - name: Sign artifacts (keyless, Windows)
        if: matrix.platform == 'windows'
        shell: pwsh
        run: |
          $files = @(
            Get-ChildItem -Path "dist/SatelliteUpscale-${{ matrix.platform }}*" -File
          )
          $files += Get-Item "dist/SHA256SUMS-${{ matrix.platform }}.txt"
          foreach ($file in $files) {
            cosign sign-blob --yes --bundle "$($file.FullName).sigstore.json" "$($file.FullName)"
          }

      - name: Attest build provenance
        uses: actions/attest-build-provenance@v3
        with:
          subject-path: |
            dist/SatelliteUpscale-${{ matrix.platform }}*
            dist/SHA256SUMS-${{ matrix.platform }}.txt
            dist/*.sigstore.json

      - name: Upload workflow artifacts
        uses: actions/upload-artifact@v4
        with:
          name: release-${{ matrix.platform }}
          path: |
            dist/SatelliteUpscale-${{ matrix.platform }}*
            dist/SHA256SUMS-${{ matrix.platform }}.txt
            dist/*.sigstore.json

      - name: Resolve release tag
        id: release_tag
        shell: bash
        run: |
          if [ "${{ github.event_name }}" = "release" ]; then
            echo "tag=${{ github.event.release.tag_name }}" >> "$GITHUB_OUTPUT"
          else
            echo "tag=${{ github.event.inputs.release_tag }}" >> "$GITHUB_OUTPUT"
          fi

      - name: Attach assets to GitHub Release
        if: github.event_name == 'release' || github.event.inputs.release_tag != ''
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.release_tag.outputs.tag }}
          files: |
            dist/SatelliteUpscale-${{ matrix.platform }}*
            dist/SHA256SUMS-${{ matrix.platform }}.txt
            dist/*.sigstore.json
